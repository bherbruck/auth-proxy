# Auth Proxy Configuration Example
# Copy this file to .env and update with your values

# Required: Username for authentication
AUTH_PROXY_USERNAME=admin

# Required: Password for authentication (use either this OR AUTH_PROXY_PASSWORD_HASH)
AUTH_PROXY_PASSWORD=your-secure-password

# Optional: Bcrypt hash of password (replaces AUTH_PROXY_PASSWORD)
# Generate with: echo "your-password" | bcrypt-tool or use online bcrypt generator
# AUTH_PROXY_PASSWORD_HASH=$2a$10$...

# Required: Target application URL to proxy to
AUTH_PROXY_TARGET=http://localhost:3000

# Required: Secret key for cookie encryption (will be auto-generated if not provided)
AUTH_PROXY_COOKIE_SECRET=your-32-character-secret-key-here

# Optional: Custom login page title (defaults to "Auth Proxy")
AUTH_PROXY_LOGIN_TITLE=My Application

# Optional: Port to run the auth proxy on (defaults to 8080)
AUTH_PROXY_PORT=8080
